import React from "react";
import { Card, CardContent, Typography } from "@material-ui/core";

export default function MediaCard(props) {
  return (
    <Card>
      <CardContent>DISPLAY IMAGE HERE and filename</CardContent>
    </Card>
  );
}
